# function(함수) 은 code 조각과 같다
# 한번 작성한 걸 가지고 계속해서 다시 쓸 수 있다

print()         # 이 function 은 파이썬에 기본적으로 포함되어 있는 것
                # 파이썬을 만든 사람들이 print 하기 위해서 이 code 를 쓴 것
                # 우리는 이 print 를 재사용하기만 하면 된다

print(True)      # print 이 값들을 console 에 나타나게 해준다
print("hello")   # 우리가 원하는 만큼 print code 를 사용하면 된다
print(12)

print(True, "hello", 12)    # ,(콤마) 를 써서 한줄로 작성해도 잘 출력된다


# 파이썬에서 function 을 만들 때 def 를 사용한다
# def 를 쓰고 한칸 띄고 "함수명"을 입력한다
# function 이름 역시 공백이 없어야하고 숫자로 시작할 수 없다
# 그 다음 괄호를 쓰고 :(콜론) 을 쓴다
# 그리고 enter 를 누르면 들여쓰기 상태가 된다

def say_hello():
  print("hello how r y?")

# code 를 실행해도 아무런 일이 일어나지 않는다
# 지금까지는 function 을 정의한 것이다
# function 을 사용하지는 않았다
# call(호출) 하지 않았다고 할 수 있다

# function 을 사용해보자
# function 안에 있는 code를 실행하기 위해서 button 을 눌러야 한다
# 그 실행 버튼은 () 이다
# function 옆에 () 를 입력하면 function 안의 code 을 실행한다

def say_hello():           # def 키워드를 쓰면 function 을 생성한다는 의미
  print("hello how r y?")

say_hello()                # function 옆에 () 를 붙여 실행하는 버튼을 만들자


print("hello world")

# "hello world" 는 ""가 오기 때문에 String(문자) 이라는 것을 배웠다
# () 가 function 안의 code 를 실행하기 위한 것을 배웠다
# 이제 () 안 값을 넣는 방법을 배워보자
