# parameter 는 function 으로 전달하는 data 를 데이터를 저장하기 위한 placeholder 라고 배웠다
# parameter 를 추가할 때는 ,(콤마) 를 쓰고 다른 parameter 를 추가하면 된다

def say_hello(name, age):                # age parameter 를 추가하여 2개의 데이터를 위한 공간을 만들었다
  print("hello", name, "how are you?")

say_hello("KARINA")                      # 첫 번째 data 는 student 가 되겠지만 두 번째 data 는 존재하지 않아서
                                         # code 를 실행하면 age 라는 필수 argument 가 없다는 error 가 발생한다
                                         # say_hello(): function 는 2 개의 data 를 받는다고 정의했기 때문이다 
                                         # call(호출) 은 call = () = 실행버튼 이다
                                         # say_hello() function 을 call 할 때 


def say_hello(name, age):                
  print("hello", name, "how are you?")
  print("you are", age, "years old")
say_hello("KARINA", 12)                  # 두 번째 argument 까지 보내준다
                                         # 보내줄 data 의 순서도 맞게 잘 작성해야한다


# function 은 작성해서 여러번 재사용이 가능한 code
# parameter 는 function 안으로 data 를 보내 함수의 결과를 바꿀 수 있게 해주 것

def tax_calculator():                    # function 이름에는 공백이 있어선 안되고, 숫자로 시작할 수도 없다

  print(10000 * 0.35)                    # function 이 실행시킬 code 는 function 안에 있어야한다
                                         # 10000 통장의 돈이며 0.35 는 세금이다
tax_calculator()                         # 이 function 을 실행하면 얼마의 세금을 내야하는지 금액이 나온다


# function 은 작성해서 여러번 재사용이 가능한 code
# parameter 는 function 안으로 data 를 보내 함수의 결과를 바꿀 수 있게 해주 것

def tax_calculator():                    # function 이름에는 공백이 있어선 안되고, 숫자로 시작할 수도 없다

  print(10000 * 0.35)                    # function 이 실행시킬 code 는 function 안에 있어야한다
                                         # 10000 통장의 돈이며 0.35 는 세금이다
tax_calculator()                         # 이 function 을 실행하면 얼마의 세금을 내야하는지 금액이 나온다


def tax_calculator(money):               # function 이 data 를 받게 하려면 place holder 가 필요하다
                                         # data를 받아줄 money 라는 parameter 를 입력해준다         
  print(money * 0.35)                    # 윗 code 는 고정된 10000 값을 사용할 수 있었지만
                                         # tax_calculator function 안의 money 라는 parameter 를 사용하여
tax_calculator(2000)                     # 여러 값을 넣어서 function 안의 code 에 활용할 수 있다
tax_calculator(3000)
